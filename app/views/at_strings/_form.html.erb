<div class="panel-body">
  <%= form_for @at_string, :html => {:class => 'panel'} do |f| %>
      <% if @at_string.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@at_string.errors.count, "error") %> prohibited this color from being saved:</h2>

            <ul>
              <% @at_string.errors.full_messages.each do |message| %>
                  <li>
                    <%= message %>
                  </li>
              <% end %>
            </ul>
          </div>
      <% end %>
      <div class="form-group">
        <%= f.label :title, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :title, :class => 'form-control' %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :value, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :value, :class => 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <div class="btn btn-warning btn-xs">
          <label class="btn btn-warning btn-sm" data-toogle="buttons">
            <%= f.check_box :edit_flag %>Press Here if someone needs to edit this key
          </label>

          <div id="Inline"></div>
        </div>
      </div>

      <% if (params[:app_id]!=nil)
           app_id = params[:app_id]
         else
           app_id = @at_string.app_id
         end %>
      <%= f.hidden_field :app_id, :value => "#{app_id}" %>
      <div class="form-actions">
        <%= f.submit nil, :class => 'btn btn-primary' %>
        <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                    at_strings_path(:app_id => app_id), :class => 'btn' %>
      </div>
  <% end %>
</div>